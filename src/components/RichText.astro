---
interface Props {
  text: string | string[];
  class?: string;
}

const { text, class: className = '' } = Astro.props;
const paragraphs = Array.isArray(text) ? text : [text];
---
{paragraphs.map((paragraph, i) => (
  <p class:list={[className, { 'mt-3': i > 0 }]}>{paragraph}</p>
))}
