---
import { ExternalLink } from 'lucide-astro';

interface TechLink {
  label: string;
  url: string;
}

interface Props {
  links?: TechLink[];
  t: any;
  heading?: string;
}

const { links, t, heading = t.common.goDeeper } = Astro.props;
---
{links && links.length > 0 && (
  <div class="mt-4 pt-4 border-t border-indigo-200">
    <p class="text-xs font-bold uppercase tracking-widest text-indigo-700 mb-3">{heading}</p>
    <div class="flex flex-wrap gap-2">
      {links.map((link) => (
        <a
          href={link.url}
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-1.5 rounded-full bg-white border border-indigo-200 px-3 py-1.5 text-sm font-semibold text-indigo-700 hover:bg-indigo-100 hover:border-indigo-300 transition-colors"
        >
          {link.label}
          <ExternalLink size={13} class="shrink-0" />
          <span class="sr-only">{t.common.opensInNewTab}</span>
        </a>
      ))}
    </div>
  </div>
)}
